<h1>New Bookmark</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, label_method: :title, value_method: :id, include_blank: 'Select a movie' %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Back to list', list_path(@list) %>
